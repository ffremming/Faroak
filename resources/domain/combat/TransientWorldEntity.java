package resources.domain.combat;

/**
 * Marker for short-lived runtime entities that may be placed while ignoring
 * terrain solidity (projectiles, flashes, impact bursts).
 */
public interface TransientWorldEntity {
}
